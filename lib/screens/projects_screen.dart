import 'package:flutter/material.dart'; import '../models/models.dart'; import '../providers/app_state.dart'; import '../widgets/common.dart';
class ProjectsScreen extends StatelessWidget{final AppState state;const ProjectsScreen({super.key,required this.state});@override Widget build(BuildContext c)=>ListView(padding:const EdgeInsets.all(24),children:[Row(children:[Expanded(child:Text('Projects',style:Theme.of(c).textTheme.headlineMedium?.copyWith(fontWeight:FontWeight.w700))),FilledButton.icon(onPressed:(){},icon:const Icon(Icons.add),label:const Text('New project'))]),const SizedBox(height:18),...state.workspace.projects.map((p)=>Padding(padding:const EdgeInsets.only(bottom:12),child:GitCard(child:ListTile(contentPadding:EdgeInsets.zero,leading:CircleAvatar(backgroundColor:Color(int.parse(p.color.substring(1),radix:16)+0xFF000000),child:const Icon(Icons.folder_open,color:Colors.white)),title:Text(p.name,style:const TextStyle(fontWeight:FontWeight.w700)),subtitle:Text('${p.description}\n${p.taskIds.length} linked tasks'),isThreeLine:true,trailing:const Icon(Icons.chevron_right)))))]);}
class KanbanScreen extends StatelessWidget{final AppState state;const KanbanScreen({super.key,required this.state});@override Widget build(BuildContext c){final cols={for(final s in TaskStatus.values)s:state.workspace.tasks.where((t)=>t.status==s).toList()};return ListView(scrollDirection:Axis.horizontal,padding:const EdgeInsets.all(24),children:[for(final s in TaskStatus.values)SizedBox(width:300,child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(enumName(s),style:Theme.of(c).textTheme.titleMedium?.copyWith(fontWeight:FontWeight.bold)),const SizedBox(height:10),...cols[s]!.map((t)=>Padding(padding:const EdgeInsets.only(bottom:10),child:GitCard(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text('#${t.id}',style:TextStyle(color:Theme.of(c).colorScheme.onSurfaceVariant)),const SizedBox(height:4),Text(t.title,style:const TextStyle(fontWeight:FontWeight.w700)),const SizedBox(height:8),StatusBadge(enumName(t.priority),Colors.blue)]))))]))]);}}
