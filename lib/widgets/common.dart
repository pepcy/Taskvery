import 'package:flutter/material.dart';
import '../models/models.dart';
class GitCard extends StatelessWidget{final Widget child;final EdgeInsets padding;const GitCard({super.key,required this.child,this.padding=const EdgeInsets.all(16)});@override Widget build(BuildContext c)=>Card(margin:EdgeInsets.zero,shape:RoundedRectangleBorder(borderRadius:BorderRadius.circular(6),side:BorderSide(color:Theme.of(c).dividerColor)),child:Padding(padding:padding,child:child));}
class StatusBadge extends StatelessWidget{final String text;final Color color;const StatusBadge(this.text,this.color,{super.key});@override Widget build(BuildContext c)=>Container(padding:const EdgeInsets.symmetric(horizontal:9,vertical:4),decoration:BoxDecoration(color:color.withOpacity(.15),border:Border.all(color:color.withOpacity(.5)),borderRadius:BorderRadius.circular(20)),child:Text(text,style:TextStyle(color:color,fontWeight:FontWeight.w600,fontSize:12)));}
class TaskRow extends StatelessWidget{final Task task;final VoidCallback onTap;const TaskRow({super.key,required this.task,required this.onTap});@override Widget build(BuildContext c){final color=task.status==TaskStatus.done?Colors.green:(task.status==TaskStatus.inProgress?Colors.amber:Colors.grey);return InkWell(onTap:onTap,child:Padding(padding:const EdgeInsets.symmetric(vertical:12),child:Row(crossAxisAlignment:CrossAxisAlignment.start,children:[Icon(task.status==TaskStatus.done?Icons.check_circle_outline:Icons.radio_button_unchecked,size:20,color:color),const SizedBox(width:12),Expanded(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text('#${task.id}  ${task.title}',style:const TextStyle(fontWeight:FontWeight.w600)),const SizedBox(height:4),Text(task.description,maxLines:1,overflow:TextOverflow.ellipsis,style:TextStyle(color:Theme.of(c).colorScheme.onSurfaceVariant)),const SizedBox(height:7),Wrap(spacing:6,runSpacing:6,children:[StatusBadge(enumName(task.status),color),StatusBadge(enumName(task.priority),task.priority==TaskPriority.high||task.priority==TaskPriority.critical?Colors.red:Colors.blue)])]))]));}}
